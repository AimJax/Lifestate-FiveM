diff --git a/apps/game/client/cl_environment.ts b/apps/game/client/cl_environment.ts
new file mode 100644
index 00000000..29c35448
--- /dev/null
+++ b/apps/game/client/cl_environment.ts
@@ -0,0 +1,94 @@
+import { RegisterNuiCB } from './cl_utils';
+
+// Read-only environment bridge for the LS One home-screen widget.
+//
+// Weather comes from the live game state (GetPrevWeatherType). FiveM exposes
+// no temperature native, so the temperature is an ESTIMATE derived from the
+// current weather plus a diurnal curve on the game clock - documented as an
+// approximation, never presented as a sensor reading.
+const WEATHER_BASE_TEMP_C: Record<string, number> = {
+  CLEAR: 28,
+  EXTRASUNNY: 31,
+  NEUTRAL: 27,
+  SMOG: 27,
+  FOGGY: 23,
+  CLOUDS: 26,
+  OVERCAST: 24,
+  RAIN: 22,
+  THUNDER: 21,
+  CLEARING: 25,
+  SNOW: 18,
+  BLIZZARD: 17,
+  SNOWLIGHT: 19,
+  XMAS: 20,
+  HALLOWEEN: 26,
+};
+
+const WEATHER_LABEL: Record<string, string> = {
+  CLEAR: 'Cerah',
+  EXTRASUNNY: 'Sangat Cerah',
+  NEUTRAL: 'Netral',
+  SMOG: 'Berasap',
+  FOGGY: 'Berkabut',
+  CLOUDS: 'Berawan',
+  OVERCAST: 'Mendung',
+  RAIN: 'Hujan',
+  THUNDER: 'Badai Petir',
+  CLEARING: 'Hujan Reda',
+  SNOW: 'Bersalju',
+  BLIZZARD: 'Badai Salju',
+  SNOWLIGHT: 'Salju Ringan',
+  XMAS: 'Natal',
+  HALLOWEEN: 'Berkabut',
+};
+
+function estimateTempC(weather: string, hour: number): number {
+  const base = WEATHER_BASE_TEMP_C[weather] ?? 26;
+  // Warmest ~14:00, coolest ~04:00, +/-3C swing.
+  const swing = -Math.cos(((hour - 4) / 24) * Math.PI * 2) * 3;
+  return Math.round(base + swing);
+}
+
+// Jenkins one-at-a-time hash (what FiveM's joaat() computes). Needed because
+// GetPrevWeatherType() hands back a HASH number, not a name string.
+function joaat(key: string): number {
+  const s = key.toLowerCase();
+  let h = 0;
+  for (let i = 0; i < s.length; i++) {
+    h += s.charCodeAt(i);
+    h += h << 10;
+    h ^= h >>> 6;
+  }
+  h += h << 3;
+  h ^= h >>> 11;
+  h += h << 15;
+  return h >>> 0;
+}
+
+const WEATHER_HASH_TO_NAME: Record<number, string> = {};
+for (const name of Object.keys(WEATHER_BASE_TEMP_C)) {
+  WEATHER_HASH_TO_NAME[joaat(name)] = name;
+}
+for (const name of Object.keys(WEATHER_LABEL)) {
+  if (!WEATHER_HASH_TO_NAME[joaat(name)]) WEATHER_HASH_TO_NAME[joaat(name)] = name;
+}
+
+// Resolve the previous weather type to a name, whether the native handed
+// back a string or a hash number. Never leaks a raw hash to the UI.
+function resolveWeather(raw: unknown): string {
+  if (typeof raw === 'string') return raw;
+  if (typeof raw === 'number') return WEATHER_HASH_TO_NAME[raw >>> 0] ?? 'CLEAR';
+  return 'CLEAR';
+}
+
+RegisterNuiCB('npwd:getEnvironment', (_, cb) => {
+  // Environment only: weather + temperature estimate. The hero clock reads
+  // NPWD's canonical phone-time store in the UI, so this payload carries no
+  // clock fields that could drift against the status bar.
+  const weather = resolveWeather(GetPrevWeatherType());
+  cb({
+    weather,
+    label: WEATHER_LABEL[weather] ?? weather,
+    tempC: estimateTempC(weather, GetClockHours()),
+  });
+});
diff --git a/apps/game/client/client.ts b/apps/game/client/client.ts
index ce874e7a..161212a7 100644
--- a/apps/game/client/client.ts
+++ b/apps/game/client/client.ts
@@ -2,6 +2,7 @@ import ClientUtils from './cl_utils';
 import './cl_config';
 
 import './cl_main';
+import './cl_environment';
 import './cl_twitter';
 import './cl_contacts';
 import './cl_marketplace';
diff --git a/apps/phone/src/Phone.css b/apps/phone/src/Phone.css
index c2a1ec57..a78c10fc 100644
--- a/apps/phone/src/Phone.css
+++ b/apps/phone/src/Phone.css
@@ -7,43 +7,100 @@
 .Phone {
   position: absolute;
   right: 0;
-  width: 500px;
-  height: 1000px;
+  width: 440px;
+  height: 860px;
   background-size: cover;
 }
 
 .PhoneFrame {
   position: absolute;
   z-index: 999;
-  width: 500px;
-  height: 1000px;
+  width: 440px;
+  height: 860px;
   pointer-events: none;
+  /* LS One: CSS graphite frame replaces the PNG handset image. */
+  background-image: linear-gradient(#D71920, #D71920) !important;
+  background-size: 3px 40px;
+  background-repeat: no-repeat;
+  background-position: calc(100% - 1px) 190px;
+  background-color: transparent;
+  border-radius: 52px;
+  border: 2px solid #282B31;
+  box-shadow:
+    inset 0 0 0 5px #111317,
+    inset 0 0 0 6px #282B31,
+    inset 0 0 0 11px #08090B,
+    0 24px 80px rgba(0, 0, 0, 0.55);
+}
+
+/* LS One punch-hole camera: fully inside the display, centered, ringed so it
+   reads on any wallpaper instead of hugging the frame edge. */
+.PhoneFrame::before {
+  content: '';
+  position: absolute;
+  top: 26px;
+  left: 50%;
+  width: 10px;
+  height: 10px;
+  margin-left: -5px;
+  border-radius: 50%;
+  background: radial-gradient(circle at 35% 35%, #23262b 0%, #000000 70%);
+  box-shadow: 0 0 0 1.5px #2a2e35, 0 0 6px rgba(0, 0, 0, 0.8);
+}
+
+/* LS One earpiece slit: subtle, in the top bezel. */
+.PhoneFrame::after {
+  content: '';
+  position: absolute;
+  top: 4px;
+  left: 50%;
+  width: 26px;
+  height: 2px;
+  margin-left: -13px;
+  border-radius: 2px;
+  background: #1e2126;
 }
 
 .PhoneScreen {
   overflow: hidden;
   position: absolute;
   background-color: black;
-  bottom: 100px;
-  left: 50px;
-  right: 50px;
-  top: 100px;
+  bottom: 12px;
+  left: 12px;
+  right: 12px;
+  top: 12px;
   display: flex;
   flex-direction: column;
   background-position: center;
   background-size: cover;
   background-repeat: no-repeat;
-  border-radius: 20px;
+  border-radius: 40px;
 }
 
 .PhoneAppContainer {
-  height: 90%;
+  height: 100%;
   width: 100%;
   display: flex;
   flex-direction: column;
   position: relative;
 }
 
+/* LS One: the phone behaves like a device, not a webpage - no accidental
+   text highlighting anywhere in the shell or apps. Editable controls opt
+   back into normal text interaction below. */
+.PhoneScreen,
+.PhoneScreen * {
+  user-select: none;
+  -webkit-user-select: none;
+}
+
+.PhoneScreen input,
+.PhoneScreen textarea,
+.PhoneScreen [contenteditable='true'] {
+  user-select: text;
+  -webkit-user-select: text;
+}
+
 * {
   box-sizing: border-box;
 }
diff --git a/apps/phone/src/apps/dialer/components/DialPadGrid.tsx b/apps/phone/src/apps/dialer/components/DialPadGrid.tsx
index 1628d816..ef08002c 100644
--- a/apps/phone/src/apps/dialer/components/DialPadGrid.tsx
+++ b/apps/phone/src/apps/dialer/components/DialPadGrid.tsx
@@ -6,8 +6,21 @@ import { DialInputCtx } from '../context/InputContext';
 
 const useStyles = makeStyles((theme: Theme) => ({
   gridItem: {
-    fontSize: theme.typography.h5.fontSize,
-    padding: theme.spacing(2),
+    fontSize: '22px',
+    fontWeight: 600,
+    minWidth: '68px',
+    minHeight: '68px',
+    width: '68px',
+    height: '68px',
+    borderRadius: '50%',
+    padding: 0,
+    margin: '0 auto',
+    display: 'flex',
+    color: '#FFFFFF',
+    backgroundColor: '#1E232D',
+    '&:hover': {
+      backgroundColor: '#292F3A',
+    },
   },
 }));
 
@@ -19,8 +32,8 @@ interface ButtonItemProps {
 const ButtonItem: React.FC<ButtonItemProps> = ({ label, onClick }) => {
   const classes = useStyles();
   return (
-    <Grid key={label} item xs={4}>
-      <Button fullWidth size="large" className={classes.gridItem} onClick={onClick}>
+    <Grid key={label} item xs={4} style={{ display: 'flex', justifyContent: 'center' }}>
+      <Button size="large" className={classes.gridItem} onClick={onClick}>
         {label}
       </Button>
     </Grid>
@@ -31,8 +44,8 @@ export const DialGrid = () => {
   const { add, removeOne, clear } = useContext(DialInputCtx);
 
   return (
-    <Box height="100%">
-      <Grid container justifyContent="space-around">
+    <Box height="100%" paddingTop={2} paddingBottom={2}>
+      <Grid container justifyContent="space-around" rowSpacing={1.5}>
         <ButtonItem label={1} onClick={() => add(1)} />
         <ButtonItem label={2} onClick={() => add(2)} />
         <ButtonItem label={3} onClick={() => add(3)} />
diff --git a/apps/phone/src/apps/dialer/components/DialerApp.tsx b/apps/phone/src/apps/dialer/components/DialerApp.tsx
index 27ca80d5..4474563c 100644
--- a/apps/phone/src/apps/dialer/components/DialerApp.tsx
+++ b/apps/phone/src/apps/dialer/components/DialerApp.tsx
@@ -1,6 +1,7 @@
 import React from 'react';
 import { AppWrapper } from '@ui/components';
 import { AppContent } from '@ui/components/AppContent';
+import { Box } from '@mui/material';
 import { DialerHistory } from './views/DialerHistory';
 import { Switch, Route } from 'react-router-dom';
 import DialPage from './views/DialPage';
@@ -15,22 +16,31 @@ export const DialerApp: React.FC = () => {
     <DialerThemeProvider>
       <AppWrapper>
         <AppContent>
-          <Switch>
-            <Route path="/phone/dial">
-              <DialPage />
-            </Route>
-            <Route exact path="/phone">
+          <Box sx={{ padding: '14px 20px 4px' }}>
+            <Box sx={{ width: '28px', height: '3px', borderRadius: '2px', backgroundColor: '#D71920', marginBottom: '8px' }} />
+            <Box sx={{ color: '#FFFFFF', fontSize: '20px', fontWeight: 800, letterSpacing: '0.5px' }}>
+              Phone
+            </Box>
+          </Box>
+          <DialerNavBar />
+          {/* Bottom clearance belongs to the global gesture pill only. */}
+          <Box sx={{ paddingBottom: '44px', flex: 1, minHeight: 0, display: 'flex', flexDirection: 'column' }}>
+            <Switch>
+              <Route path="/phone/dial">
+                <DialPage />
+              </Route>
+              <Route exact path="/phone">
+                <React.Suspense fallback={<LoadingSpinner />}>
+                  <DialerHistory />
+                </React.Suspense>
+              </Route>
               <React.Suspense fallback={<LoadingSpinner />}>
-                <DialerHistory />
+                <Route path="/phone/contacts" component={ContactList} />
               </React.Suspense>
-            </Route>
-            <React.Suspense fallback={<LoadingSpinner />}>
-              <Route path="/phone/contacts" component={ContactList} />
-            </React.Suspense>
-          </Switch>
+            </Switch>
+          </Box>
         </AppContent>
-        <DialerNavBar />
       </AppWrapper>
     </DialerThemeProvider>
   );
-};
\ No newline at end of file
+};
diff --git a/apps/phone/src/apps/dialer/components/DialerInput.tsx b/apps/phone/src/apps/dialer/components/DialerInput.tsx
index 521b593c..e06a8e5c 100644
--- a/apps/phone/src/apps/dialer/components/DialerInput.tsx
+++ b/apps/phone/src/apps/dialer/components/DialerInput.tsx
@@ -1,5 +1,5 @@
 import React, { useContext } from 'react';
-import { Box, IconButton, Paper } from '@mui/material';
+import { Box, IconButton } from '@mui/material';
 import { Theme } from '@mui/material/styles';
 import makeStyles from '@mui/styles/makeStyles';
 import PhoneIcon from '@mui/icons-material/Phone';
@@ -13,20 +13,51 @@ import { toggleKeys } from '@ui/components/Input';
 
 const useStyles = makeStyles((theme: Theme) => ({
   root: {
-    textAlign: 'right',
-    paddingTop: theme.spacing(3),
-    paddingBottom: theme.spacing(3),
-    paddingLeft: theme.spacing(5),
-    paddingRight: theme.spacing(5),
+    textAlign: 'center',
+    paddingTop: theme.spacing(4),
+    paddingBottom: theme.spacing(2),
+    paddingLeft: theme.spacing(3),
+    paddingRight: theme.spacing(3),
     display: 'flex',
-    height: 100,
+    flexDirection: 'column',
+    alignItems: 'center',
+    background: 'transparent',
   },
   input: {
     flex: 1,
-    fontSize: theme.typography.h6.fontSize,
+    width: '100%',
+    fontSize: '34px',
+    fontWeight: 700,
+    letterSpacing: '2px',
+    color: '#FFFFFF',
+    textAlign: 'center',
+    '& input': {
+      textAlign: 'center',
+    },
+  },
+  actions: {
+    display: 'flex',
+    gap: theme.spacing(3),
+    marginTop: theme.spacing(2),
+    alignItems: 'center',
+    justifyContent: 'center',
+  },
+  callBtn: {
+    padding: 16,
+    backgroundColor: '#22C55E',
+    color: '#FFFFFF',
+    '&:hover': {
+      backgroundColor: '#22C55E',
+      filter: 'brightness(1.1)',
+    },
+    '&.Mui-disabled': {
+      backgroundColor: '#1E232D',
+      color: '#6F7885',
+    },
   },
   iconBtn: {
-    padding: 10,
+    padding: 12,
+    color: '#9AA3AF',
   },
 }));
 
@@ -47,32 +78,34 @@ export const DialerInput: React.FC = () => {
   };
 
   return (
-    <Box component={Paper} className={classes.root}>
+    <Box className={classes.root}>
       <InputBase
         placeholder={t('DIALER.INPUT_PLACEHOLDER')}
         className={classes.input}
         value={inputVal}
         onChange={(e) => set(e.target.value)}
       />
-      <IconButton
-        color="primary"
-        className={classes.iconBtn}
-        disabled={inputVal <= ''}
-        onClick={() => handleCall(inputVal)}
-        size="large"
-      >
-        <PhoneIcon fontSize="large" />
-      </IconButton>
-      <IconButton
-        className={classes.iconBtn}
-        onClick={() => handleNewContact(inputVal)}
-        onMouseUp={() => {
-          toggleKeys(false);
-        }}
-        size="large"
-      >
-        <PersonAddIcon fontSize="large" />
-      </IconButton>
+      <div className={classes.actions}>
+        <IconButton
+          className={classes.iconBtn}
+          onClick={() => handleNewContact(inputVal)}
+          onMouseUp={() => {
+            toggleKeys(false);
+          }}
+          size="large"
+        >
+          <PersonAddIcon fontSize="medium" />
+        </IconButton>
+        <IconButton
+          className={classes.callBtn}
+          disabled={inputVal <= ''}
+          onClick={() => handleCall(inputVal)}
+          size="large"
+        >
+          <PhoneIcon fontSize="medium" />
+        </IconButton>
+        <Box width={48} />
+      </div>
     </Box>
   );
 };
diff --git a/apps/phone/src/apps/dialer/components/DialerNavBar.tsx b/apps/phone/src/apps/dialer/components/DialerNavBar.tsx
index 2e4cb99b..c9a05b1b 100644
--- a/apps/phone/src/apps/dialer/components/DialerNavBar.tsx
+++ b/apps/phone/src/apps/dialer/components/DialerNavBar.tsx
@@ -1,57 +1,58 @@
-import React, { useState } from 'react';
-import { BottomNavigation, BottomNavigationAction } from '@mui/material';
+import React from 'react';
 import { NavLink, useLocation } from 'react-router-dom';
-import { Theme } from '@mui/material/styles';
-import makeStyles from '@mui/styles/makeStyles';
-import PhoneIcon from '@mui/icons-material/Phone';
-import PersonIcon from '@mui/icons-material/Person';
-import HistoryIcon from '@mui/icons-material/History';
-import { useTranslation } from 'react-i18next';
-import { Contact, History, Phone } from 'lucide-react';
+import { Box } from '@mui/material';
 
-const useStyles = makeStyles((theme: Theme) => ({
-  root: {
-    width: '100%',
-    backgroundColor: theme.palette.background.paper,
-  },
-  icon: {
-    color: theme.palette.primary.main,
-  },
-}));
+// LS One top segmented tabs: Riwayat / Panggilan / Kontak. Replaces the
+// legacy bottom bar (which collided with the global gesture pill). Routes
+// and behavior are unchanged - only presentation moved.
+const TABS = [
+  { label: 'Riwayat', to: '/phone', exact: true },
+  { label: 'Panggilan', to: '/phone/dial', exact: false },
+  { label: 'Kontak', to: '/phone/contacts', exact: false },
+];
 
 const DialerNavBar: React.FC = () => {
-  const classes = useStyles();
   const { pathname } = useLocation();
-  const [page, setPage] = useState(pathname);
-  const [t] = useTranslation();
 
-  const handleChange = (_e, newPage) => {
-    setPage(newPage);
-  };
+  const isActive = (to: string, exact: boolean) =>
+    exact ? pathname === to : pathname === to || pathname.startsWith(to + '/');
 
   return (
-    <BottomNavigation value={page} onChange={handleChange} showLabels className={classes.root}>
-      <BottomNavigationAction
-        value="/phone"
-        component={NavLink}
-        icon={<History />}
-        to="/phone"
-      />
-      <BottomNavigationAction
-        value="/phone/dial"
-        color="secondary"
-        component={NavLink}
-        icon={<Phone />}
-        to="/phone/dial"
-      />
-      <BottomNavigationAction
-        value="/phone/contacts"
-        color="secondary"
-        component={NavLink}
-        icon={<Contact />}
-        to="/phone/contacts"
-      />
-    </BottomNavigation>
+    <Box
+      component="nav"
+      sx={{
+        display: 'flex',
+        gap: '4px',
+        padding: '4px 16px 0',
+        borderBottom: '1px solid #292F3A',
+      }}
+    >
+      {TABS.map((tab) => {
+        const active = isActive(tab.to, tab.exact);
+        return (
+          <Box
+            key={tab.to}
+            component={NavLink}
+            to={tab.to}
+            exact={tab.exact}
+            sx={{
+              flex: 1,
+              textAlign: 'center',
+              textDecoration: 'none',
+              fontSize: '13px',
+              fontWeight: 700,
+              letterSpacing: '0.5px',
+              padding: '10px 4px 12px',
+              color: active ? '#FFFFFF' : '#6F7885',
+              borderBottom: active ? '2px solid #D71920' : '2px solid transparent',
+              marginBottom: '-1px',
+            }}
+          >
+            {tab.label}
+          </Box>
+        );
+      })}
+    </Box>
   );
 };
 
diff --git a/apps/phone/src/apps/dialer/components/views/DialerHistory.tsx b/apps/phone/src/apps/dialer/components/views/DialerHistory.tsx
index 5f62bbb4..dd9a5429 100644
--- a/apps/phone/src/apps/dialer/components/views/DialerHistory.tsx
+++ b/apps/phone/src/apps/dialer/components/views/DialerHistory.tsx
@@ -35,16 +35,37 @@ export const DialerHistory: React.FC = () => {
   if (!calls?.length) {
     return (
       <Box display="flex" justifyContent="center" alignItems="center" paddingTop={35}>
-        <p>
-          {
-            <Typography style={{ color: phoneTheme.palette.text.primary }}>
-              {t('DIALER.NO_HISTORY')}
-            </Typography>
-          }
-          <span role="img" aria-label="sad">
-            😞
-          </span>
-        </p>
+              <Box
+        display="flex"
+        flexDirection="column"
+        justifyContent="center"
+        alignItems="center"
+        paddingTop={10}
+        textAlign="center"
+      >
+        <Box
+          sx={{
+            width: '64px',
+            height: '64px',
+            borderRadius: '18px',
+            backgroundColor: '#181C24',
+            border: '1px solid #292F3A',
+            display: 'flex',
+            alignItems: 'center',
+            justifyContent: 'center',
+            marginBottom: '16px',
+            color: '#6F7885',
+          }}
+        >
+          <History size={28} />
+        </Box>
+        <Typography style={{ color: '#FFFFFF', fontSize: '16px', fontWeight: 700 }}>
+          Belum ada riwayat
+        </Typography>
+        <Typography style={{ color: '#9AA3AF', fontSize: '12px', marginTop: '6px' }}>
+          Panggilan masuk dan keluar akan muncul di sini.
+        </Typography>
+      </Box>
       </Box>
     );
   }
diff --git a/apps/phone/src/apps/home/components/Home.tsx b/apps/phone/src/apps/home/components/Home.tsx
index 40d749e9..cb492695 100644
--- a/apps/phone/src/apps/home/components/Home.tsx
+++ b/apps/phone/src/apps/home/components/Home.tsx
@@ -1,33 +1,139 @@
-import React from 'react';
+import React, { useEffect, useState } from 'react';
 import { AppWrapper } from '@ui/components';
+import { AppIcon } from '@ui/components/AppIcon';
 import { Box } from '@mui/material';
+import { Link } from 'react-router-dom';
+import { useTranslation } from 'react-i18next';
 import { GridMenu } from '@ui/components/GridMenu';
 import { useApps } from '@os/apps/hooks/useApps';
 import { useExternalApps } from '@common/hooks/useExternalApps';
+import { IApp } from '@os/apps/config/apps';
+import usePhoneTime from '@os/phone/hooks/usePhoneTime';
+import fetchNui from '@utils/fetchNui';
+
+// LS One dock: exactly these core apps live in the hotbar, in this order.
+// Everything else (including dynamic external apps) stays in the grid.
+const DOCK_APP_IDS = ['DIALER', 'MESSAGES', 'CONTACTS', 'CAMERA'];
+
+interface EnvState {
+  label: string;
+  tempC: number;
+}
+
+// LS One home widget: the hero clock reads the SAME recoil time source as
+// the status bar, so both always agree. Weather/temp come from the read-only
+// game bridge, refreshed every minute. Nothing renders until real data
+// arrives, so the home screen never shows placeholder values.
+const HomeWidget: React.FC = () => {
+  const phoneTime = usePhoneTime();
+  const [env, setEnv] = useState<EnvState | null>(null);
+
+  useEffect(() => {
+    let alive = true;
+    const load = () => {
+      fetchNui<EnvState>('npwd:getEnvironment')
+        .then((data) => {
+          if (alive && data) setEnv(data);
+        })
+        .catch(() => {});
+    };
+    load();
+    const timer = setInterval(load, 60000);
+    return () => {
+      alive = false;
+      clearInterval(timer);
+    };
+  }, []);
+
+  if (!phoneTime || !env) return null;
+
+  return (
+    <Box sx={{ padding: '6px 8px 0', marginTop: '40px', textAlign: 'center' }}>
+      <Box sx={{ color: '#F5F7FA', fontSize: '44px', fontWeight: 700, letterSpacing: '1px', lineHeight: 1.1, fontVariantNumeric: 'tabular-nums' }}>
+        {phoneTime}
+      </Box>
+      <Box sx={{ color: '#9AA3AF', fontSize: '12px', fontWeight: 600, marginTop: '4px' }}>
+        {env.label} &middot; {env.tempC}&deg;C
+      </Box>
+    </Box>
+  );
+};
+
+const DockApp: React.FC<{ app: IApp }> = ({ app }) => {
+  const [t] = useTranslation();
+  return (
+    <Link
+      to={app.path}
+      style={{
+        textDecoration: 'none',
+        display: 'flex',
+        flexDirection: 'column',
+        alignItems: 'center',
+        gap: '4px',
+      }}
+      key={app.id}
+    >
+      <AppIcon {...app} dense />
+      <Box
+        component="div"
+        sx={{
+          color: '#cfd4da',
+          fontSize: '9.5px',
+          fontWeight: 600,
+          lineHeight: '12px',
+          maxWidth: '56px',
+          whiteSpace: 'nowrap',
+          overflow: 'hidden',
+          textOverflow: 'ellipsis',
+          textAlign: 'center',
+        }}
+      >
+        {t(app.nameLocale)}
+      </Box>
+    </Link>
+  );
+};
 
 export const HomeApp: React.FC = () => {
   const { apps } = useApps();
   const externalApps = useExternalApps();
+  const allApps = [...(apps || []), ...(externalApps || [])];
+  const dockApps = DOCK_APP_IDS.map((id) => allApps.find((app) => app && app.id === id)).filter(
+    (app): app is IApp => !!app && !app.isDisabled,
+  );
+  const dockIds = new Set(dockApps.map((app) => app.id));
+  const gridApps = allApps.filter((app) => app && !dockIds.has(app.id));
+
   return (
     <AppWrapper>
-      <Box component="div" mt={6} px={1}>
-        {apps && <GridMenu xs={3} items={[...apps, ...externalApps]} />}
+      <HomeWidget />
+      <Box
+        component="div"
+        mt={2}
+        px={2}
+        sx={{ flex: 1, minHeight: 0, overflowY: 'auto', display: 'flex', flexDirection: 'column' }}
+      >
+        <Box sx={{ marginTop: 'auto', paddingBottom: '16px' }}>
+          {apps && <GridMenu xs={3} items={gridApps} />}
+        </Box>
+      </Box>
+      <Box display="flex" justifyContent="center" pb="40px" pt={1}>
+        <Box
+          display="flex"
+          alignItems="flex-start"
+          sx={{
+            gap: '18px',
+            padding: '12px 20px 10px',
+            borderRadius: '24px',
+            backgroundColor: 'rgba(20, 23, 29, 0.85)',
+            border: '1px solid #292F3A',
+          }}
+        >
+          {dockApps.map((app) => (
+            <DockApp key={app.id} app={app} />
+          ))}
+        </Box>
       </Box>
-
-      {/*<div className="absolute bottom-5 left-8 right-8">
-        <div className="h-20 w-full rounded-md bg-gray-200/30 backdrop-blur">
-          {apps &&
-            apps.slice(0, 4).map((app) => (
-              <div className="float-left h-full w-1/4" key={app.id}>
-                <div className="flex h-full w-full items-center justify-center">
-                  <div className="flex h-14 w-14 items-center justify-center rounded-md bg-gray-200/50 backdrop-blur">
-                    {app.icon}
-                  </div>
-                </div>
-              </div>
-            ))}
-        </div>
-      </div>*/}
     </AppWrapper>
   );
 };
diff --git a/apps/phone/src/config/default.json b/apps/phone/src/config/default.json
index 07639e94..ddaf9a40 100644
--- a/apps/phone/src/config/default.json
+++ b/apps/phone/src/config/default.json
@@ -3,28 +3,8 @@
   "defaultLanguage": "en",
   "wallpapers": [
     {
-      "label": "Minimal",
-      "value": "minimal.jpg"
-    },
-    {
-      "label": "Surf",
-      "value": "surf.jpg"
-    },
-    {
-      "label": "Waves",
-      "value": "waves.jpg"
-    },
-    {
-      "label": "Project Error",
-      "value": "projecterror.jpg"
-    },
-    {
-      "label": "NPWD 2020",
-      "value": "npwd2020.png"
-    },
-    {
-      "label": "Default",
-      "value": "default.jpg"
+      "label": "LS One",
+      "value": "lsone.png"
     }
   ],
   "frames": [
@@ -217,8 +197,8 @@
       }
     },
     "wallpaper": {
-      "label": "Minimal",
-      "value": "minimal.jpg"
+      "label": "LS One",
+      "value": "lsone.png"
     },
     "customWallpaper": "",
     "frame": {
diff --git a/apps/phone/src/os/apps/config/apps.tsx b/apps/phone/src/os/apps/config/apps.tsx
index e8bb1a7a..7df228bc 100644
--- a/apps/phone/src/os/apps/config/apps.tsx
+++ b/apps/phone/src/os/apps/config/apps.tsx
@@ -81,9 +81,9 @@ export const APPS: IAppConfig[] = [
   {
     id: 'DIALER',
     nameLocale: 'APPS_DIALER',
-    backgroundColor: DIALER_APP_PRIMARY_COLOR,
+    backgroundColor: '#22C55E',
     icon: <DialerAppIcon />,
-    color: DIALER_APP_TEXT_COLOR,
+    color: common.white,
     path: '/phone',
     Route: () => <AppRoute id="DIALER" path="/phone" component={DialerApp} emitOnOpen={false} />,
   },
@@ -102,8 +102,8 @@ export const APPS: IAppConfig[] = [
     id: 'MESSAGES',
     nameLocale: 'APPS_MESSAGES',
     icon: <MessagesIcon />,
-    backgroundColor: MESSAGES_APP_PRIMARY_COLOR,
-    color: MESSAGES_APP_TEXT_COLOR,
+    backgroundColor: '#14B8A6',
+    color: common.white,
     path: '/messages',
     Route: () => (
       <AppRoute id="MESSAGES" path="/messages" component={MessagesApp} emitOnOpen={false} />
@@ -123,9 +123,9 @@ export const APPS: IAppConfig[] = [
   {
     id: 'CONTACTS',
     nameLocale: 'APPS_CONTACTS',
-    backgroundColor: CONTACTS_APP_PRIMARY_COLOR,
+    backgroundColor: '#3B82F6',
     icon: <ContactIcon />,
-    color: CONTACTS_APP_TEXT_COLOR,
+    color: common.white,
     path: '/contacts',
     Route: () => (
       <AppRoute id="CONTACTS" path="/contacts" component={ContactsApp} emitOnOpen={false} />
@@ -135,8 +135,8 @@ export const APPS: IAppConfig[] = [
     id: 'CALCULATOR',
     nameLocale: 'APPS_CALCULATOR',
     icon: <Calculator />,
-    backgroundColor: purple[500],
-    color: grey[50],
+    backgroundColor: '#8B5CF6',
+    color: common.white,
     path: '/calculator',
     Route: () => (
       <AppRoute id="CALCULATOR" path="/calculator" component={CalculatorApp} emitOnOpen={false} />
@@ -146,8 +146,8 @@ export const APPS: IAppConfig[] = [
     id: 'SETTINGS',
     nameLocale: 'APPS_SETTINGS',
     icon: <SettingsIcon />,
-    backgroundColor: '#383838',
-    color: grey[50],
+    backgroundColor: '#23262B',
+    color: common.white,
     path: '/settings',
     Route: () => (
       <AppRoute id="SETTINGS" path="/settings" component={SettingsApp} emitOnOpen={false} />
@@ -193,8 +193,8 @@ export const APPS: IAppConfig[] = [
     id: 'NOTES',
     nameLocale: 'APPS_NOTES',
     icon: <NotesIcon />,
-    backgroundColor: NOTES_APP_PRIMARY_COLOR,
-    color: NOTES_APP_ICON_COLOR,
+    backgroundColor: '#F59E0B',
+    color: common.white,
     path: '/notes',
     Route: () => <AppRoute id="NOTES" path="/notes" component={NotesApp} emitOnOpen={false} />,
   },
@@ -202,7 +202,7 @@ export const APPS: IAppConfig[] = [
     id: 'CAMERA',
     nameLocale: 'APPS_CAMERA',
     icon: <Camera />,
-    backgroundColor: grey['A400'],
+    backgroundColor: '#3A3F47',
     color: common.white,
     path: '/camera',
     Route: () => <AppRoute id="CAMERA" path="/camera" component={CameraApp} emitOnOpen={false} />,
diff --git a/apps/phone/src/os/apps/icons/material/app/CALCULATOR.tsx b/apps/phone/src/os/apps/icons/material/app/CALCULATOR.tsx
index 6918049c..cf571da4 100644
--- a/apps/phone/src/os/apps/icons/material/app/CALCULATOR.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/CALCULATOR.tsx
@@ -1,6 +1,14 @@
 import React from 'react';
-import { Calculator as LCalc } from 'lucide-react';
 
-const Calculator: React.FC = () =><LCalc size={30} />;
+// LS One system glyph: calculator body with display + key grid knocked out
+// via evenodd, so it stays monochrome on any tile color.
+const Calculator: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path
+      fillRule="evenodd"
+      d="M8.5 2h7c1.4 0 2.5 1.1 2.5 2.5v15c0 1.4-1.1 2.5-2.5 2.5h-7c-1.4 0-2.5-1.1-2.5-2.5v-15C6 3.1 7.1 2 8.5 2zm-.5 3v3h8V5H8zm0 5v2h2v-2H8zm3 0v2h2v-2h-2zm3 0v2h2v-2h-2zM8 14v2h2v-2H8zm3 0v2h2v-2h-2zm3 0v2h2v-2h-2z"
+    />
+  </svg>
+);
 
 export default Calculator;
diff --git a/apps/phone/src/os/apps/icons/material/app/CAMERA.tsx b/apps/phone/src/os/apps/icons/material/app/CAMERA.tsx
index 745da68d..85ae6cc7 100644
--- a/apps/phone/src/os/apps/icons/material/app/CAMERA.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/CAMERA.tsx
@@ -1,6 +1,10 @@
 import React from 'react';
-import { Camera as LCamera } from 'lucide-react';
 
-const Camera: React.FC = () => <LCamera size={30} />;
+// LS One system glyph: camera body with lens ring, filled geometry.
+const Camera: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path d="M12 15.2c1.77 0 3.2-1.43 3.2-3.2s-1.43-3.2-3.2-3.2-3.2 1.43-3.2 3.2 1.43 3.2 3.2 3.2zM9 2L7.17 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2h-3.17L15 2H9zm3 15c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5z" />
+  </svg>
+);
 
 export default Camera;
diff --git a/apps/phone/src/os/apps/icons/material/app/CONTACTS.tsx b/apps/phone/src/os/apps/icons/material/app/CONTACTS.tsx
index ac245dfb..8760e790 100644
--- a/apps/phone/src/os/apps/icons/material/app/CONTACTS.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/CONTACTS.tsx
@@ -1,6 +1,10 @@
 import React from 'react';
-import { BookUser } from 'lucide-react';
 
-const ContactIcon: React.FC = () => <BookUser size={30} />;
+// LS One system glyph: person silhouette, filled geometry.
+const ContactIcon: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z" />
+  </svg>
+);
 
 export default ContactIcon;
diff --git a/apps/phone/src/os/apps/icons/material/app/DIALER.tsx b/apps/phone/src/os/apps/icons/material/app/DIALER.tsx
index 685f4562..41c8eb9a 100644
--- a/apps/phone/src/os/apps/icons/material/app/DIALER.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/DIALER.tsx
@@ -1,6 +1,11 @@
 import React from 'react';
-import { Phone } from 'lucide-react';
 
-const DialerIcon: React.FC = () => <Phone size={30} />;
+// LS One system glyph: classic handset silhouette, filled geometry matching
+// the Lifestate brand-mark language. Rendered in currentColor (white on tile).
+const DialerIcon: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z" />
+  </svg>
+);
 
 export default DialerIcon;
diff --git a/apps/phone/src/os/apps/icons/material/app/MESSAGES.tsx b/apps/phone/src/os/apps/icons/material/app/MESSAGES.tsx
index a54c73ac..ec508948 100644
--- a/apps/phone/src/os/apps/icons/material/app/MESSAGES.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/MESSAGES.tsx
@@ -1,6 +1,10 @@
 import React from 'react';
-import { MessageSquare } from 'lucide-react';
 
-const MessagesIcon: React.FC = () => <MessageSquare size={30} />;
+// LS One system glyph: rounded chat bubble, filled geometry.
+const MessagesIcon: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2z" />
+  </svg>
+);
 
 export default MessagesIcon;
diff --git a/apps/phone/src/os/apps/icons/material/app/NOTES.tsx b/apps/phone/src/os/apps/icons/material/app/NOTES.tsx
index d6231ef3..7fb8880a 100644
--- a/apps/phone/src/os/apps/icons/material/app/NOTES.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/NOTES.tsx
@@ -1,6 +1,10 @@
 import React from 'react';
-import { StickyNote } from 'lucide-react';
 
-const NotesIcon: React.FC = () => <StickyNote size={30} />
+// LS One system glyph: pencil, filled geometry.
+const NotesIcon: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path d="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04c.39-.39.39-1.02 0-1.41l-2.34-2.34c-.39-.39-1.02-.39-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z" />
+  </svg>
+);
 
 export default NotesIcon;
diff --git a/apps/phone/src/os/apps/icons/material/app/SETTINGS.tsx b/apps/phone/src/os/apps/icons/material/app/SETTINGS.tsx
index 3eb38180..95780fe4 100644
--- a/apps/phone/src/os/apps/icons/material/app/SETTINGS.tsx
+++ b/apps/phone/src/os/apps/icons/material/app/SETTINGS.tsx
@@ -1,6 +1,11 @@
 import React from 'react';
-import { Settings } from 'lucide-react';
 
-const SettingsIcon: React.FC = () => <Settings size={30} />;
+// LS One system glyph: proper gear, filled geometry matching the home icon
+// family. Replaces the confusing sliders mark.
+const SettingsIcon: React.FC = () => (
+  <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
+    <path d="M19.14 12.94c.04-.3.06-.61.06-.94 0-.32-.02-.64-.07-.94l2.03-1.58c.18-.14.23-.41.12-.61l-1.92-3.32c-.12-.22-.37-.29-.59-.22l-2.39.96c-.5-.38-1.03-.7-1.62-.94l-.36-2.54c-.04-.24-.24-.41-.48-.41h-3.84c-.24 0-.43.17-.47.41l-.36 2.54c-.59.24-1.13.57-1.62.94l-2.39-.96c-.22-.08-.47 0-.59.22L2.74 8.87c-.12.21-.08.47.12.61l2.03 1.58c-.05.3-.09.63-.09.94s.02.64.07.94l-2.03 1.58c-.18.14-.23.41-.12.61l1.92 3.32c.12.22.37.29.59.22l2.39-.96c.5.38 1.03.7 1.62.94l.36 2.54c.05.24.24.41.48.41h3.84c.24 0 .44-.17.47-.41l.36-2.54c.59-.24 1.13-.56 1.62-.94l2.39.96c.22.08.47 0 .59-.22l1.92-3.32c.12-.22.08-.47-.12-.61l-2.01-1.58zM12 15.6c-1.98 0-3.6-1.62-3.6-3.6s1.62-3.6 3.6-3.6 3.6 1.62 3.6 3.6-1.62 3.6-3.6 3.6z" />
+  </svg>
+);
 
 export default SettingsIcon;
diff --git a/apps/phone/src/os/navigation-bar/components/Navigation.tsx b/apps/phone/src/os/navigation-bar/components/Navigation.tsx
index ef3d27c1..2bf5a0cb 100644
--- a/apps/phone/src/os/navigation-bar/components/Navigation.tsx
+++ b/apps/phone/src/os/navigation-bar/components/Navigation.tsx
@@ -1,35 +1,24 @@
 import React from 'react';
 import { useHistory, useRouteMatch } from 'react-router-dom';
-import { usePhone } from '@os/phone/hooks/usePhone';
-import { ChevronLeft, Circle, LayoutGrid } from 'lucide-react';
 
 export const Navigation: React.FC = () => {
   const history = useHistory();
   const { isExact } = useRouteMatch('/');
-  const { closePhone } = usePhone();
-
-  const handleGoBackInHistory = () => {
-    history.goBack();
-  };
 
   const handleGoToMenu = () => {
     if (isExact) return;
+    // Home only: the gesture pill is the single visible control. Back
+    // navigation lives in per-app headers; closing uses the phone toggle.
     history.push('/');
   };
 
   return (
-    <div className="bg-neutral-100 dark:bg-neutral-900 w-full h-14 px-12">
-      <div className="flex justify-between items-center h-full">
-        <button onClick={handleGoToMenu}>
-          <LayoutGrid className="text-neutral-400 hover:dark:text-neutral-100 h-6 w-6 hover:text-neutral-900 " />
-        </button>
-        <button onClick={closePhone}>
-          <Circle className="text-neutral-400 hover:dark:text-neutral-100 h-6 w-6 hover:text-neutral-900 " />
-        </button>
-        <button onClick={handleGoBackInHistory}>
-          <ChevronLeft className="text-neutral-400 hover:dark:text-neutral-100 hover:text-neutral-900 h-6 w-6" />
-        </button>
-      </div>
+    /* LS One gesture bar: one floating pill, no bar background, no legacy
+       buttons. Overlays the content safe-area; pointer events only on it. */
+    <div className="absolute bottom-3 left-0 right-0 z-50 flex justify-center pointer-events-none">
+      <button onClick={handleGoToMenu} aria-label="Home" className="pointer-events-auto px-6 py-2">
+        <span className="block h-[5px] w-24 rounded-full bg-white/85" />
+      </button>
     </div>
   );
 };
diff --git a/apps/phone/src/os/new-notifications/components/NotificationBar.tsx b/apps/phone/src/os/new-notifications/components/NotificationBar.tsx
index f8abbec5..5a603a87 100644
--- a/apps/phone/src/os/new-notifications/components/NotificationBar.tsx
+++ b/apps/phone/src/os/new-notifications/components/NotificationBar.tsx
@@ -34,13 +34,18 @@ import {cn} from "@utils/css";
 
 const useStyles = makeStyles((theme) => ({
   root: {
-    backgroundColor: theme.palette.background.default,
-    height: '30px',
+    /* LS One: transparent status bar integrated into the display instead of
+       a separate dark slab; contents sit ~5px lower for a balanced top area.
+       The notification drawer below keeps its surface. */
+    backgroundColor: 'transparent',
+    height: '35px',
     width: '100%',
     color: theme.palette.text.primary,
     zIndex: 99,
-    paddingLeft: '15px',
-    paddingRight: '15px',
+    paddingLeft: '20px',
+    paddingRight: '20px',
+    paddingTop: '4px',
+    boxSizing: 'border-box',
     position: 'relative',
     '&:hover': {
       cursor: 'pointer',
@@ -62,7 +67,7 @@ const useStyles = makeStyles((theme) => ({
     backgroundColor: theme.palette.background.default,
     width: '100%',
     position: 'absolute',
-    top: '30px',
+    top: '35px',
     zIndex: 98,
   },
   closeNotifBtn: {
@@ -142,21 +147,25 @@ export const NotificationBar = () => {
           setBarUncollapsed((curr) => !curr);
         }}
       >
-        <Grid container item wrap="nowrap">
-          {unreadNotifications &&
-            unreadNotifications
-              .filter((val, idx, self) => idx === self.findIndex((t) => t.appId === val.appId))
-              .map((notification, idx) => {
-                return <IconUnreadGrid tgtNoti={notification} key={idx} />;
-              })}
-        </Grid>
-        {time && (
-          <Grid item className={classes.item}>
-            <Typography className={classes.text} variant="button">
-              {time}
-            </Typography>
+        {/* LS One: time + notification icons left-aligned, keeping the top
+            center clear for the punch-hole camera. */}
+        <div className="flex items-center flex-nowrap">
+          {time && (
+            <Grid item className={classes.item}>
+              <Typography className={classes.text} variant="button">
+                {time}
+              </Typography>
+            </Grid>
+          )}
+          <Grid container item wrap="nowrap">
+            {unreadNotifications &&
+              unreadNotifications
+                .filter((val, idx, self) => idx === self.findIndex((t) => t.appId === val.appId))
+                .map((notification, idx) => {
+                  return <IconUnreadGrid tgtNoti={notification} key={idx} />;
+                })}
           </Grid>
-        )}
+        </div>
         <div className="flex items-center justify-end">
           <div>
             <SignalMedium />
diff --git a/apps/phone/src/ui/components/AppIcon.tsx b/apps/phone/src/ui/components/AppIcon.tsx
index 4fbd5f91..8e5b17e0 100644
--- a/apps/phone/src/ui/components/AppIcon.tsx
+++ b/apps/phone/src/ui/components/AppIcon.tsx
@@ -1,5 +1,5 @@
 import React from 'react';
-import { darken, Theme } from '@mui/material/styles';
+import { Theme } from '@mui/material/styles';
 import makeStyles from '@mui/styles/makeStyles';
 import { green } from '@mui/material/colors';
 import { Badge, Button, Zoom } from '@mui/material';
@@ -7,35 +7,32 @@ import { useTranslation } from 'react-i18next';
 import { INotificationIcon } from '@os/notifications/providers/NotificationsProvider';
 import { Tooltip } from './Tooltip';
 
-const useStyles = makeStyles<Theme, { color: string; backgroundColor: string }>((theme) => ({
+const useStyles = makeStyles<Theme, { color: string; backgroundColor: string; dense: boolean }>((theme) => ({
   root: {
     padding: 0,
     background: 'transparent',
-    marginTop: theme.spacing(3),
+    marginTop: ({ dense }) => (dense ? 0 : theme.spacing(3)),
   },
   avatar: {
     '&:hover': {
-      background: ({ backgroundColor }) => {
-        return `linear-gradient(45deg, ${darken(backgroundColor, 0.25)} 10%, ${backgroundColor} 90%)`;
-      },
-    },
-    background: ({ backgroundColor }) => {
-      return `linear-gradient(45deg, ${darken(backgroundColor, 0.2)} 20%, ${backgroundColor} 90%)`;
+      filter: 'brightness(1.12)',
     },
+    background: ({ backgroundColor }) => backgroundColor,
     color: ({ color }) => color,
-    boxShadow: theme.shadows[2],
+    border: '1px solid rgba(255, 255, 255, 0.09)',
+    boxShadow: '0 4px 14px rgba(0, 0, 0, 0.35)',
     display: 'flex',
     justifyContent: 'center',
     alignItems: 'center',
-    borderRadius: 18,
-    width: theme.spacing(8),
-    height: theme.spacing(8),
+    borderRadius: 15,
+    width: 52,
+    height: 52,
     fontSize: theme.typography.h4.fontSize,
   },
   icon: {
     fontSize: theme.typography.h4.fontSize,
-    width: theme.spacing(8),
-    height: theme.spacing(8),
+    width: 52,
+    height: 52,
   },
   tooltip: {
     fontSize: 12,
@@ -50,6 +47,7 @@ export interface AppIconProps {
   backgroundColor: string;
   color: string;
   notification: INotificationIcon;
+  dense?: boolean;
 }
 
 export const AppIcon: React.FC<AppIconProps> = ({
@@ -60,11 +58,13 @@ export const AppIcon: React.FC<AppIconProps> = ({
   color,
   icon,
   notification,
+  dense,
 }) => {
   const [t] = useTranslation();
   const classes = useStyles({
     backgroundColor: backgroundColor || green[50],
     color: color || green[400],
+    dense: dense || false,
   });
 
   return (
diff --git a/apps/phone/src/ui/components/GridMenu.tsx b/apps/phone/src/ui/components/GridMenu.tsx
index d99e2b3d..6e3c0c55 100644
--- a/apps/phone/src/ui/components/GridMenu.tsx
+++ b/apps/phone/src/ui/components/GridMenu.tsx
@@ -2,6 +2,7 @@ import React, { Fragment } from 'react';
 import { AppIcon } from './AppIcon';
 import { Box, Grid, GridSize } from '@mui/material';
 import { Link } from 'react-router-dom';
+import { useTranslation } from 'react-i18next';
 import { IApp } from '@os/apps/config/apps';
 
 interface GridMenuProps {
@@ -11,8 +12,9 @@ interface GridMenuProps {
 }
 
 export const GridMenu: React.FC<GridMenuProps> = ({ items, Component = AppIcon, xs }) => {
+  const [t] = useTranslation();
   return (
-    <Grid container alignItems="center" direction="row">
+    <Grid container alignItems="flex-start" direction="row">
       {items &&
         items.length &&
         items.map((item) => (
@@ -20,8 +22,24 @@ export const GridMenu: React.FC<GridMenuProps> = ({ items, Component = AppIcon,
             {!item.isDisabled && (
               <Grid item xs={xs} key={item.id}>
                 <Box textAlign="center">
-                  <Link to={item.path}>
+                  <Link to={item.path} style={{ textDecoration: 'none' }}>
                     <Component {...item} />
+                    <Box
+                      component="div"
+                      sx={{
+                        color: '#cfd4da',
+                        fontSize: '10px',
+                        fontWeight: 600,
+                        lineHeight: '13px',
+                        marginTop: '6px',
+                        padding: '0 2px',
+                        whiteSpace: 'nowrap',
+                        overflow: 'hidden',
+                        textOverflow: 'ellipsis',
+                      }}
+                    >
+                      {t(item.nameLocale)}
+                    </Box>
                   </Link>
                 </Box>
               </Grid>
